[pytest]
testpaths = optio tests
# addopts = --tb=no
addopts = '--tb=short'
python_files = optio/*.py tests/*.py

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning

# addopts = '--html=test_report.html' '--self-contained-html'
